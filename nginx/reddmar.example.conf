# Ejemplo: Nginx en el host (Ubuntu) con TLS en la EC2.
# Ajusta server_name, rutas de certificados y rutas a proxy.
# El front Docker escucha en 127.0.0.1:8080; el API Nest en 127.0.0.1:3000.

# Upstreams (opcional)
# map $http_upgrade $connection_upgrade { default upgrade; '' close; }

# server {
#     listen 80;
#     server_name www.reddmar.com reddmar.com;
#     return 301 https://www.reddmar.com$request_uri;
# }

# server {
#     listen 443 ssl http2;
#     server_name www.reddmar.com;
#
#     ssl_certificate     /etc/letsencrypt/live/www.reddmar.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/www.reddmar.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# API: mismo host o subdominio (recomendado https://api.reddmar.com sin :3000 público)
# server {
#     listen 443 ssl http2;
#     server_name api.reddmar.com;
#
#     ssl_certificate     /etc/letsencrypt/live/api.reddmar.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.reddmar.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
# }
